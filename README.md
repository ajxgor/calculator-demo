# Calculator Demo

A simple Python calculator with GitHub Actions CI.

## Features
- Add, subtract, multiply, divide
- Unit tests with pytest
- Lint checks with flake8
- CI via GitHub Actions

## Run Tests
```bash
pip install pytest
pytest test_calculator.py -v
```
